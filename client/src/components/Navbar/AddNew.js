import React, { Component } from 'react'

class AddNew extends Component {
    render() {
        return (
            <button>
                Add Boards
            </button>
        )
    }
}

export default AddNew;