import React, { Component } from 'react'

class Boards extends Component {
    render() {
        return (
            <button>
                Boards
            </button>
        )
    }
}

export default Boards;