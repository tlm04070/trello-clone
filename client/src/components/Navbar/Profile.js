import React, { Component } from 'react'

class Profile extends Component {
    render() {
        return (
            <button>
                Profile
            </button>
        )
    }
}

export default Profile;